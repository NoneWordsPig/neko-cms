# neko 更新公告：她现在能记住“相关的过去”

发布日期：2026-09-16

大家好，这次给聪明山上的 neko 做了一次记忆系统更新。简单说：她以后不只是“看到眼前这一条消息”，还可以从过去的对话里找出与当前话题相关的内容。

## 这次有什么变化？

- **看得见 AI bot 的发言，但不会被 bot 触发回复。** neko 和其他 AI 的聊天会保留在上下文里，所以她可以评价另一个 bot，也能知道自己刚刚说过什么；同时，机器人之间不会因为互相接话而叠楼。
- **公共记忆库**记录更新启用后产生的大区消息，以及新发布博客的标题和引言。第一次启动时不会把过去所有大区消息和旧博客突然倒灌进来。
- **私人记忆库**按私聊对象分开保存。每位和 neko 私聊的人都有自己的数据库，首次启用时会回填这段私聊历史，之后继续保留双方的新消息。不同用户之间不会混用记忆。
- 记忆会长期保存，不会因为超过 24 小时就删除。旧 JSONL 归档也会保留，方便迁移和备份。

## “向量化搜索”到底是什么？

可以把它想成给每段文字放到一张“语义地图”上：意思相近的句子，通常会被放得更近。

1. 一条消息进入记忆库时，会被转换成一串数字，也就是向量。
2. neko 收到新消息时，也把新消息转换成向量。
3. 系统计算新旧向量的夹角（余弦相似度），挑出最接近的几条旧记忆。
4. 这些记忆会作为“参考资料”放进提示词，供 neko 判断和组织回答；它们不是新的指令。

当前默认方案不依赖外部服务：使用中文单字、相邻双字和英数字词做特征，再通过哈希生成 **384 维本地向量**。它对同词、近似写法和短句很实用，部署也比较稳。若以后配置 OpenAI 兼容的 embedding 服务，新记忆可以改用更强的语义向量；服务暂时不可用时会自动退回本地方案，不会因此阻塞回复。

## 24 小时还重要吗？

重要，但不再是“过期就删除”。现在检索仍以相似度为主，最近 24 小时的记忆只获得一个很小的额外分数（目前是 **+0.08**）。所以：

- 最近发生且相关的内容更容易被想起；
- 很久以前但明显相关的内容仍然可能被检索到；
- “刚发生但完全无关”的消息不会轻易压过真正相关的旧记忆。

每次最多注入 10 条、约 1200 字，避免上下文变得太拥挤。

## 记录范围和隐私边界

- 公共库只从功能启用后开始收集大区消息；博客只记录标题和引言，不保存博客全文。
- 私人库只在当前私聊对象的数据库内检索，不会拿其他人的私聊内容来回答你。
- 数据库文件不提交到 GitHub，但会留在部署机器上；实际部署仍需要做好备份、文件权限和按需删除管理。
- 向量搜索不是“完美记忆”：措辞特别隐晦、内容太短或本地哈希碰撞时，可能找不到最理想的旧消息。

## 接下来想做什么？

如果大家觉得有用，后续可以继续加上：记忆时间线、用户主动保存/遗忘某条记忆、对话主题总结，以及更强的语义 embedding。欢迎在山上告诉我们：你希望 neko 记住什么，又希望她在哪些时候忘掉什么。

